# Autumn for Codex / other agents

Read `CLAUDE.md` for all project instructions, the seven-file context
system, and behavioral rules. They apply equally to every agent.

Skills are installed via [skills.sh](https://skills.sh) and live in
`.agents/skills/` (canonical) with symlinks at `.claude/skills/`. Both
directories load the same content; pick whichever your agent reads.

For Codex- or tool-specific overrides, append them below this line.
